
"""Simple calculator module used in the Copilot demo.

Start by adding functions from natural language comments and let Copilot propose code.
"""
from typing import Iterable

# TODO: Write a function add(a: float, b: float) -> float that returns the sum.
# Include a short docstring and basic type hints.


# TODO: Now add a function mean(numbers: Iterable[float]) -> float
# that returns the arithmetic average. If the input is empty, raise ValueError.

# TODO (later): Add a function clamp(value, low, high) that returns value bounded to [low, high].

if __name__ == "__main__":
    # Small manual demo once functions exist
    print("add(2, 3) =", "<to be implemented>")
