
# Exercise 03 — Tests with pytest

Open `src/python/test_calculator.py`. Generate tests with Chat:

- `Write pytest unit tests for add and mean covering positive, negative, floats, and empty list edge cases.`
- `Parametrize the tests to reduce duplication.`
- `Add a test for clamp(value, low, high).`

Run tests:
```bash
pytest -q
```
Fix failures by improving the implementation (ask Chat for help if needed).
