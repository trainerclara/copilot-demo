
# Exercise 01 — Inline Generation (calculator.py)

Open `src/python/calculator.py` and start from natural language comments.

Prompts you can type as comments:

1. `Write a function add(a: float, b: float) -> float that returns the sum. Include a short docstring.`
2. `Add a function mean(numbers: Iterable[float]) -> float that raises ValueError on empty input.`
3. `Add a function clamp(value, low, high) that bounds the value to [low, high].`

Next, ask Copilot to:
- Improve type hints and error handling.
- Add docstrings in Google style.
- Suggest edge cases.

> Tip: Type slowly and cycle suggestions (Alt/⌥ + ]). Choose one and refine in Chat.
