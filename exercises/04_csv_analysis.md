
# Exercise 04 — CSV analysis with pandas

Open `src/python/data_tasks.py` and complete the TODO with Chat:

- `Write summarize_sales(df: pd.DataFrame) -> pd.DataFrame that computes revenue = units * price, groups by region and month (YYYY-MM), sorts by revenue desc, and returns top 10 rows.`
- `Add a bar chart of revenue by region using matplotlib.`
- `Handle malformed rows gracefully and document assumptions.`

Run:
```bash
python src/python/data_tasks.py
```
